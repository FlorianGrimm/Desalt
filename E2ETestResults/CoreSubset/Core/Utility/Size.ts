/**
 * Basic W,H size.  Use SizeUtil to manipulate.
 */
export class Size {
  public w: number;

  public h: number;

  public constructor(width: number, height: number) {
    this.w = width;
    this.h = height;
  }
}

/**
 * Basic W,H size.  Use SizeUtil to manipulate.
 */
export class SizeD {
  public w: number;

  public h: number;

  public constructor(width: number, height: number) {
    this.w = width;
    this.h = height;
  }
}
